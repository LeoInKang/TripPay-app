# TripPay 개선 백로그 (테스터 피드백)

_비공개 테스트 중 수집한 피드백 정리. 정식 출시(프로덕션) 이후 업데이트 반영 예정._

---

## 1. 유연한 비용 분담 ⭐ 최우선 (테스터 2명 요청)

**현재 동작**: 지출을 참석자 전원에게 1/N(엔빵)으로 균등 분배.

**요청 내용**
- 지출별로 **실제 참여한 사람만** 나누기 (그 지출에 빠진 사람은 제외)
- 1/N 고정이 아니라 **사람별 비율 / 배수 / 직접 금액** 지정
- 위 내용을 모두 반영해 **개인별로 정확히 정산**

**정리(구현 방향)**: 지출마다 `참여자 목록 + 분담 방식(균등 / 비율 / 배수 / 고정액)`을 선택 → 정산 계산에 반영. (Splitwise 스타일)

**원문**
- 테스터 A: "지출도 일부 인원 빠질 수 있으니 참석자만 엔빵이 되면 좋고 … 각 개인별로 정산하게"
- 테스터 B: "비용 부담을 무조건 1/N 말고 사람별 비율 또는 배수 등을 넣어서 할 수 있도록"

---

## 코드 확인 결과 (SettleTab.js)
- **현재 정산 = 남은 잔액 ÷ 인원수(N)**. `refundPer = totalBalKrw / N` → 모든 참석자에게 **동일 금액** 표시.
- **개인별 회비 납부액을 계산에 전혀 사용하지 않음** → 불균등 회비가 정산에 **반영 안 됨** (많이 낸 사람도 똑같이 환급).
- 지출은 전원 공동 부담 가정 → **지출별 참여자 개념 없음**.
- 회비 입력 자체는 사람별·건별 다른 금액 가능하나, **정산이 이를 무시**함.

## 개선 방향 (핵심)
정산을 **개인별 순액 방식**으로 재설계:
`개인 환급/부담 = (본인이 낸 돈: 회비·충전 등) − (본인 몫 지출)`
- 지출마다 `참여자 + 분담 방식(균등/비율/배수/고정액)` 지정 → 개인 몫 계산
- 개인별 낸 돈 합계 반영 → 많이 낸 사람이 더 돌려받도록

---

## 2. 여행 국가(통화) 추가 (테스터 요청)

**요청**: 지원 국가/통화 확대 — 홍콩, 인도네시아, 미얀마.

**처리**: `SetupScreen.js`의 `COUNTRIES` 배열에 3개국 추가 완료 (코드 반영, 다음 빌드에 포함 예정)
- 🇭🇰 홍콩 HKD `HK$` (기본환율 178)
- 🇮🇩 인도네시아 IDR `Rp` (기본환율 0.085)
- 🇲🇲 미얀마 MMK `K` (기본환율 0.65)
- ※ 환율은 앱 내 수정 가능한 기본값. 인도네시아·미얀마는 변동 커서 대략값.

**향후 검토**: 사용자가 직접 국가 추가하는 커스텀 기능 (단, IDR/MMK처럼 소수점 환율·통화단위 입력 오류 위험 있어 신중히)

---

## 구현 상태 (2026-07 · (b) 최종 스코프 반영 완료)
최종 방향: **불균등 회비 + 유연한 나눔**만 남기고, 선결제/대납/결제자 관련은 제거(운영으로 처리).
- `settle.js` — 개인별 정산 엔진 (순액 = 회비 − 부담). 결제자 크레딧 없음. 단위 테스트 15개 통과.
- `components/SplitEditor.js` — 참여자 선택 + 분담방식(균등/비율/배수/고정액), 기본 접힘(전원 균등).
- `SettleTab.js` — 참석자별 개인 순액 표시(돌려받기/더 내기). 공금 잔액 로직 원복(정상).
- `AddTab.js`, `ListTab.js` — 지출 추가·수정에 나눔 편집기 연결. **결제자 필드 완전 제거**.
- 신규 participants/split은 `storage.js`로 자동 저장. 기존 데이터는 전원 균등으로 호환.
- 여행 국가 3개(홍콩·인니·미얀마) 추가됨.

### 선결제/대납 처리 방침 (기능 아님, 운영 가이드)
원칙 하나: **대납액은 장부에 딱 한 번만 나타나야 한다.** 상황별 처리는 세 가지.

① **공금이 넉넉함 (기본)** — 입금 기록 없음.
   지출 1건 기록(참여자 = 실제 그 비용을 쓴 사람들) + 공금에서 대납자에게 지불. 대납자는 본전.
② **공금 부족** — 전원 **균등 추가 회비 징수**(입금 기록) → 지불 + 지출 1건 기록.
③ **정산까지 안 갚음** — 대납액을 **그 사람의 회비**로 기록 + 지출 1건 기록 → 순액에서 자동으로 돌려받음.

- **입금과 부담은 독립이다.** 지출의 참여자·분담방식은 ①②③ 공통으로 실제 사용 기준(균등일 필요 없음). 회비를 누가 얼마 냈는지는 부담과 무관하고, 그 차이는 순액 정산이 자동으로 상쇄한다 — ②의 "균등 징수"도 공정성 장치가 아니라 걷기 편한 방식일 뿐이다.
- 지출의 결제수단은 대납자가 뭘로 냈는지가 아니라 **공금이 실제로 어떻게 나갔는지**로 적는다(계좌이체 갚음 = 원화 지출, 현지 현금 갚음 = 외화 지출·현금). 잔액 3종이 실제와 맞으려면 이게 기준.
- 대납이 반복돼 불편하다는 피드백이 쌓이면 "결제자 필드" 기능화 재검토 (순액 = 회비 + 대납액 − 부담. 잔액 계산에서 대납 지출 제외 + 갚음 처리 상태 전이가 필요해서 SplitEditor급 작업).

**⚠️ 개인 카드로 결제한 대납을 "카드 지출 + 충전" 짝으로 적지 말 것.**
앱의 계좌·카드·현금은 전부 총무의 공금 지갑이다. 공용 카드에 일어나지 않은 일을 기록하면
카드 이력이 오염될 뿐 아니라, **가짜 충전이 평균환율(getAvgRate)에 들어가 전원의 환산 금액을
미세하게 틀어놓는다.** 개인 카드 대납은 위 ①②③의 원화 경로로만 처리한다.
충전 기록이 정당한 경우는 **공용 카드에 실제로 돈이 들어갔을 때뿐**이다.

**충전·환전을 개인 돈으로 대납한 경우** (공용 카드에 실제 충전이 일어났고, 자금만 개인 돈인 케이스)
- 충전 기록은 이미 "계좌에서 나간 것"으로 잡혀 있다는 게 핵심.
- **정산 전에 갚으면** → 공금 계좌에서 송금만 하고 **아무것도 추가 기록하지 않는다.** 충전 기록이 곧 그 차감이므로, 지출로 또 적으면 이중차감.
- **안 갚고 정산까지 가면** → 대납액을 그 사람의 **원화 회비 1건**으로 기록(메모: 충전 대납). 낸 돈이 늘어 순액에 자동 반영되고, 계좌 잔액도 실제와 일치하게 된다.
- 메모("지급필요")는 운영용 표시일 뿐 정산 엔진은 읽지 않는다.

### 지출 일부만 공금 부담 (기능 아님, 운영 가이드)
- 상황: 지출 한 건에서 **일부는 특정 인원만, 나머지는 공금**이 부담하게 하고 싶을 때.
- 전제: 공금은 별도 지갑이 아니라 참석자들이 낸 회비다 → **"공금 부담" = "전원 균등 부담"**. 별도 개념이 필요 없다.
- 방법: **지출을 두 건으로 나눠 입력**한다.
  - `골프 그린피 60,000` — 참여자 A·B
  - `골프 공용비 40,000` — 참여자 전원
  - 결과(A·B 부담 40,000 / C·D 부담 10,000)는 한 건에 고정액으로 직접 넣은 것과 **완전히 동일**. 계산이 필요 없고 내역에 근거가 남는다.
- 한 건 안에서 "일부만 개인 부담"은 지원하지 않는다. 부담 합계 < 지출액이면 공금에서 나간 돈이 아무에게도 안 잡혀 정산 총합이 실제 잔액과 어긋나기 때문. 고정액 저장 시 `checkFixedSplit`이 막는다.
- SplitEditor 안내 문구에 이 방법을 적어 뒀다.

## 2026-08-04 수정 (정산 정확도)
- **평균환율 단일 출처화** — `settle.js`의 `getAvgRate(trip, charges, exchanges)`를 Home·Settle·Deposit 세 탭이 함께 쓴다. 실거래(충전·환전) 0건이면 `country.exRate`로 폴백.
  - 고친 문제 ①: 현황 탭은 저장된 `krwEquiv`, 정산 탭은 현재 평균환율을 써서 **외화 회비가 있으면 두 탭의 '총 입금'이 달랐다.**
  - 고친 문제 ②: 충전·환전이 0건인 여행(환전 없이 외화 현금만 걷은 경우)에서 정산 쪽 환율이 0이 되어 **외화 회비가 0원으로 잡히고 순액이 뒤집혔다.**
  - 현황 탭 '총 지출'도 정산 탭과 같은 건별 환산 후 합으로 통일.
- **분담 합계 검증 — 고정액·비율 공통 규칙으로 통일.** `splitErrorMessage`가 저장을 막는다(지출 추가 2곳 + 내역 수정 모달). 접힌 편집기 헤드에도 부족·초과 배지 표시.
  - 고정액(`checkFixedSplit`): 합계 = 지출액. 보정이 없어 어긋나면 공금에서 나간 돈이 아무에게도 안 잡힌다.
  - 비율(`checkRatioSplit`): 합계 = 100%. 엔진이 총 가중치로 정규화해서 **계산은 원래도 맞았지만**, 30/30/30을 넣으면 각자 33.3%를 물어 **입력값과 실제가 달랐다.** 세 모드 중 유일하게 어긋나던 지점이라 고정액과 같은 규칙으로 맞췄다.
  - **'100%로 맞추기' 버튼**(`normalizeRatio`) — 비를 유지한 채 합계를 정확히 100으로 정규화. 2:1:1 → 50/25/25, 1:1:1 → 33.34/33.33/33.33. 100% 강제로 잃는 배수 입력을 이 버튼으로 되살렸다(엔진의 `shares` 모드는 여전히 UI 미노출).
  - 비율 각 행에 실제 부담 금액을 함께 표시.
  - 엔진(`settle.js`)의 계산 로직은 그대로다. UI 제약만 추가한 것이라 기존 데이터의 정산 결과는 안 바뀐다.
  - ※ 이미 어긋나게 저장된 기존 지출은 자동으로 고쳐지지 않는다. 해당 지출을 수정할 때 차단되며 드러난다.
- 단위 테스트 20 → **54개**(`node test-settle.mjs`).

### 영수증 자동입력 (v1.2 후보 · 2026-08-05 실증)
- 실증: 실제 여행 영수증 20장(렌터카 정산서·골프장·마트·식당·주차·숙박)을 Claude에 올려 파싱 →
  가져오기 JSON 생성 → 앱 가져오기. **확인 질문 1개(골프장 영수증이 1인분인지 합계인지), 수정 0건.**
- 현재 워크플로우: 사진 → Claude(코워크) → JSON 파일 → AirDrop → 앱 "여행 데이터 가져오기".
- **모델 중립**: 프롬프트 템플릿 `docs/receipt-import-prompt.md` — ChatGPT·제미나이에 붙여넣어도 같은 JSON이 나온다. v1.2에서는 워커의 업로더 페이지(`/upload`: 붙여넣기 → 클라이언트 암호화 → QR)로 어떤 AI든 동일 UX.
- **프롬프트 복사 UI는 v5.1에서 제외** (2026-08-06): 수동 경로(복사→AI→파일→AirDrop→가져오기)가 일반 사용자에겐 과하게 번거롭다는 판단. 코드·의존성(expo-clipboard) 제거 완료. 사용자용 영수증 입력은 v1.2 링크 방식으로 처음 공개한다.
- **v1.2 구현 완료 (2026-08-06): "클립보드 가져오기"** — 링크·서버 설계를 검토하다 더 단순한 답을 찾았다.
  AI가 만든 JSON은 이미 사용자 클립보드에 있으므로 **서버를 거칠 이유가 없다.**
  - 사용자 흐름: 앱 "AI로 영수증 입력" → [프롬프트 복사] → AI에 사진과 붙여넣기 → 결과 코드블록 [복사]
    → 앱으로 돌아와 [클립보드에서 가져오기] → 여행 선택 → 미리보기 → 추가. **복사 버튼 두 번이 전부.**
  - 네트워크 불필요(오프라인 동작), 데이터가 기기 밖으로 안 나감, 신고·방침 영향 0.
  - 구현: `screens/ImportAIScreen.js` + `receiptPrompt.js`(parseAiJson — 코드펜스·잡담 제거).
    대상 여행 선택(통화 일치 우선, 새 여행 생성 지원), 통화 불일치 차단, 내용 지문 batchId로
    같은 묶음 재추가 차단, 지출 id 재발급. 웹 미리보기로 신규 생성·중복 차단 검증 완료.
  - 워커의 `/i`·`/upload`·`/r`(링크 전송로)은 **기기 간 전송용 예비 경로로 유지** — 배포돼 있으나
    앱은 의존하지 않는다. 코워크(Mac)→폰 전송이 필요해지면 그때 앱에 링크 붙여넣기를 추가.
  - **실기기 리허설 피드백 반영 (2026-08-06)**: ① 지출(+) 탭 상단에도 진입 버튼 — 이 경로는
    현재 여행에 자동 추가(여행 선택 질문 없음, 통화 불일치는 여전히 차단). ② 랜딩 진입은 기존대로
    여행 선택·새 여행 생성 UI 유지(임의 자동 선택은 과잉이라는 피드백). ③ 추가 직후 내역 탭으로
    이동해 새 지출에 "새로 추가" 배지·파란 배경 하이라이트(일회성 표시, 저장 안 함).

---

## 신용카드 결제 · 결제수단 정리 (2026-08-07)

- **'트레블월렛' → '트래블카드'로 전면 교체.** 표기 정정 + 특정 브랜드명 회피. 이름의 단일 출처는
  `constants.js`(`PAY_CARD`)이고, 화면에 문자열을 직접 쓰지 않는다.
  - 이미 테스터 기기에 저장된 데이터가 있으므로 **로드 시 자동 변환**한다: `migrate.js`의
    `migrateTripData`가 `expenses`·`krwExps`의 `pay`와 `trip.payMethods`를 바꾸고,
    `storage.loadTripData` · `transfer.importTripFile` · `ImportAIScreen`(AI 결과) 세 경로에서 통과시킨다.
    변환분은 MainScreen 자동저장이 다음 저장 때 기록한다. 실측 확인: 옛 데이터 로드 → 화면·저장분 모두 새 이름.

- **결제수단에 '신용카드' 추가.** 차감처가 수단별로 갈린다:
  트래블카드 → 카드 외화 잔액 / 현금 → 현금 잔액 / **신용카드 → 계좌**.
  - 잔액 코드가 `pay !== '현금'`으로 카드 지출을 고르고 있어서 그대로 두면 신용카드가 트래블카드
    잔액에서 빠진다. `HomeTab`·`SettleTab` 양쪽에서 `&& pay !== PAY_CREDIT`을 함께 본다.
    결제수단이 빈 구버전 데이터는 종전대로 카드 결제로 취급한다.

- **확정 원화(`krwActual`)** — 해외 신용카드는 원화 청구액이 3~5영업일 뒤 매입 시점 환율로 정해진다.
  여행 중에는 외화 금액만 넣고 평균환율로 추정하다가, 확정되면 내역 수정에서 실제 청구액을 넣는다.
  - **외화 `amt`는 항상 보존**한다. 통화를 바꾸는 게 아니라 그 건에만 적용되는 실효 환율을 주는 것이다.
  - 환산은 `settle.js`의 `expenseKrw`/`makeExpToKrw` 하나만 쓴다(화면에서 `toKrw(e.amt)` 직접 호출 금지 —
    확정 원화가 무시돼 정산과 화면이 어긋난다). 고정액 분담도 실효 환율로 환산돼 부담 합계가 정확히 맞는다.
  - 표시: 내역에 확정 전 **확정대기 배지 + 추정액**, 확정 후 **"₩N 확정"**. 정산 탭에는 확정대기 건이
    있을 때만 안내 배너. 도움말에 "신용카드로 결제했을 때" 카드 추가.
  - 엔진을 고쳤으므로 **공유 뷰어(`view.html`)에도 같은 규칙을 이식**했다. 회귀 테스트 21번, 67/67 통과.

- **지출 입력 폼 통합** — 외화·원화 카드 2개를 하나로 합치고 통화를 Segment로 고른다.
  저장 위치만 갈리고(외화 → `expenses`, 원화 → `krwExps`) 데이터 구조·정산 규칙은 그대로다.
  확정 원화 칸은 **외화 + 신용카드**일 때만 나온다(원화 지출은 이미 원화라 의미가 없다).

- 잔액 마이너스 경고에 "자동충전 내역이 빠지지 않았는지 확인해 보세요" 한 줄 추가(현황·정산 공통).

- **영수증 AI는 카드 종류를 구분하지 못한다 — 트래블카드로 고정하고 변경은 사용자가 한다.**
  트래블카드는 마스터/비자 결제망을 그대로 타서 영수증에 `MASTERCARD 7814`처럼 브랜드와 끝
  4자리만 찍힌다. 일본의 "일시불", 서구의 "CREDIT" 표시도 선불카드에 그대로 붙어 단서가 안 된다.
  AI가 확실히 가르는 건 **현금이냐 카드냐**까지다(2026-08-05 영수증 20장 실증에서도 카드 종류는 되물었다).
  - 끝 4자리를 미리 알려주면 자동 분류가 가능하지만 **사용자가 애매하다고 판단** → 채택하지 않았다.
  - 결론: 프롬프트는 `현금`·`트래블카드` 둘만 쓰게 하고 카드 종류는 아예 묻지 않는다.
    신용카드 건은 사용자가 내역 수정에서 바꾸고, 그때 확정 원화 칸이 함께 나타난다.
    가져오기 화면 안내에도 이 내용을 적었다. 프롬프트 사본 세 곳(`receiptPrompt.js` ·
    `docs/receipt-import-prompt.md` · 워커 `/upload`)은 문자 단위로 동일한지 확인했다.
  - 잘못 분류돼도 **정산 총액·개인 부담은 틀리지 않는다.** 어느 잔액에서 빠지느냐만 달라진다
    (실측: 트래블카드 → 신용카드로 바꾸니 카드 C$-120 → C$0, 계좌 -537,000 → -654,600원).

- **프롬프트에서 되묻기·요약을 걷어냈다 (2026-08-07, 제미나이·클로드 실물 검증 후).**
  결과물이 너무 길고 선택지를 많이 물어본다는 피드백. 원인은 AI 특성이 아니라 **프롬프트가 그걸 시키고
  있어서**였다 — 규칙 7(참석자 물어봐) · 8(애매하면 질문해) · 9(끝에 확인용 요약을 붙여 줘).
  - **참석자**: 지출 탭 경로에서는 AI가 적은 `members`를 앱이 아예 쓰지 않는다(현재 여행 참석자를 쓴다).
    프롬프트에서 빼고 스키마 예시만 `["총무"]`로 뒀다.
  - **확인용 요약(9번) 삭제**: 앱 미리보기가 건수·통화별 합계를 이미 보여준다.
  - **"1인분/합계 애매하면 질문"은 애초에 불필요했다.** `amt`는 규칙 1대로 항상 영수증의 최종 합계이고,
    누가 얼마씩 부담할지는 앱의 분담방식이 정한다. 대신 `@9,250 × 3 = 27,750` 같은 영수증에서 1인분을
    집는 실수를 막으려고 규칙 1에 "여러 명 몫이 한 장에 찍혀 있어도 합계를 쓴다"를 못박았다.
  - **판독 불가만 남겼고, 그것도 되묻지 않는다**: 그 영수증만 빼고 JSON을 만든 뒤 뺀 것을 한 줄로 알린다.
    → 사용자가 AI 대화로 되돌아가 다시 받는 왕복이 사라진다.
  - 맨 앞에 `출력은 JSON 코드블록 하나만. 인사말·설명·요약·표는 쓰지 마.` 추가.
  - ※ 모델은 설명하려는 성향이 있어 100% 보장은 안 된다. `parseAiJson`이 앞뒤 잡담을 걷어내므로
    길게 나와도 통째로 붙여넣으면 들어간다(제미나이 전체 응답으로 실측 확인).

- **참석자 없는 JSON에 앱이 죽던 버그 수정.** `members`를 빼고 가져오면 `MainScreen`의
  `trip.members.join(', ')`에서 흰 화면으로 죽었다(실측). 프롬프트로 막을 문제가 아니다 —
  모델이 바뀌거나 출력이 잘리면 언제든 재발한다.
  - `migrate.js`가 참석자가 없거나 비면 `['총무']`로 채운다(가져오기 세 경로 공통). `join`도 방어적으로 바꿨다.
  - 덕분에 프롬프트에서 참석자 규칙을 뺄 수 있었다 — **AI가 뭘 주든 앱이 안 죽는 게 기준.**

- **앱 내장 비전(카메라→API 파싱)은 무료 배포와 상성이 나쁘다** — 링크 방식 다음 단계로만.
  - 품질 자체는 문제없다: 파싱 주체가 같은 LLM 비전이라 2026-08-05 실증과 동일. 단, 대화형 질문이
    불가능하므로 모델이 확신도를 함께 반환하고 앱이 **미리보기 폼에서 불확실 필드를 표시**하는 설계가 필요
    (전통 OCR·영수증 특화 파서는 렌터카 정산서의 예정/정산/예치금 구분, "받은 돈 vs 합계" 같은 판단을 못 한다).
  - 비용: 장당 ~5원(Haiku급)~15원(Sonnet급). 사용자가 늘수록 상한 없이 커지고, 공개 프록시라
    스크립트 남용에도 노출된다. **수익 0인 앱에서 무제한 무료 제공은 불가.**
  - 전제 조건: ① 워커에 API 키 프록시 + 기기별 호출 제한, ② 데이터 보안 신고에 "사진 수집" 추가,
    ③ **유료화 결정**(예: 월 N장 무료 + 스캔팩) — Play 결제 구현·수수료가 따라오는 사업 결정.
  - 링크 방식(v1.2)은 파싱을 사용자 본인의 AI 계정이 하므로 개발자 비용이 구조적으로 0원 —
    무료 배포에서는 이쪽이 기본이고, 내장 비전은 "AI 계정 없는 사용자 커버 + 유료화 각오"가
    동시에 성립할 때만 꺼낸다.

### 남은 일
- EAS 새 빌드 → 비공개 테스트에 올려 기기 검증 → 이상 없으면 출시 반영.
- 후속(선택): 웹 공유 페이지(share.js)도 개인별 순액 반영.

## 처리 상태
- 대상 버전: **v1.1 (정식 출시 후)** → 이번 출시 빌드에 포함 예정
- 지금 테스트 빌드는 그대로 두고, 프로덕션 출시 후 업데이트로 반영 (업데이트는 테스터 재모집 불필요)
